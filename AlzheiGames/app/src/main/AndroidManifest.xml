<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.alzheigames">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AsistenciaActivity"></activity>
        <activity android:name=".MedicoActivity"></activity>
        <activity android:name=".medico_verResultado" />
        <activity android:name=".Cuidador_verResultado" />
        <activity android:name=".MenuJuegosActivity" />
        <activity android:name=".RegistroCuidadorActivity" />
        <activity android:name=".RegistroMedicoActivity" />
        <activity android:name=".MenuRegistroActivity" />
        <activity android:name=".RegistroPacienteActivity" />
        <activity android:name=".CuidadorActivity" />
        <activity android:name=".PacienteActivity" />
        <activity android:name=".PrincipalActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>